# TODO — VD Transparency Button blocking burger menu on mobile

## Steps
- [x] Edit `victoria-diamonds.css`: move the `.floating-transparency-btn` to the bottom-right corner on mobile (≤900px) so it no longer overlaps the burger menu.
- [x] Move the VD Transparency button to the bottom-right corner on desktop too (base rule uses `bottom: 1.25rem`).
- [ ] Verify in a mobile viewport that the burger menu is clickable and the transparency button is still visible/functional.
